[project]
name = "kanji-api"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "uvicorn>=0.34.0",
]

[project.scripts]
api = "kanji_api:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"